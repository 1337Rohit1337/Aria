apiVersion: apps/v1
kind: Deployment
metadata:
  name: aria-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: aria-redis
  template:
    metadata:
      labels:
        app: aria-redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: aria-redis
spec:
  ports:
    - port: 6379
  selector:
    app: aria-redis